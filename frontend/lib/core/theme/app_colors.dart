import 'package:flutter/material.dart';

class AppColors {
  // Colores principales (Extraídos de la identidad antigua)
  static const Color primaryBlue = Color(0xFF003087);
  static const Color backgroundWhite = Color(0xFFFFF4EE);
  static const Color primaryYellow = Color(0xFFFFCD00);
  static const Color errorRed = Color(0xFFC8102E);

  // Colores adicionales para la UI Premium (Escala de grises, sombras, etc.)
  static const Color textDark = Color(0xFF0F0D0D);
  static const Color textLight = Color(0xFF5A5A5A);
  static const Color glassmorphismBorder = Color(
    0x33FFFFFF,
  ); // Borde sutil para glassmorphism
  static const Color glassmorphismFill = Color(
    0x1AFFFFFF,
  ); // Fondo sutil blanco translúcido
}
